{
  "name": "yennifer-facturas",
  "version": "1.0.0",
  "description": "Jeniffer — control interno de facturas, pagos e ISLR (aplicativo local)",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "build": "npm run build:backend && npm run build:frontend",
    "build:vercel": "cd backend && npx prisma generate && npx prisma db push && npm run build && cd ../frontend && env VITE_API_URL= npm run build",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "db:push": "cd backend && npx prisma db push",
    "db:seed": "cd backend && npm run db:seed",
    "db:import": "bash scripts/importar.sh",
    "db:bcv-rebuild": "cd backend && npx tsx scripts/rebuild-bcv-historico.ts",
    "docker:build": "docker build -t jeniffer .",
    "docker:run": "docker run --rm -p 8000:8000 -v jeniffer-koyeb-data:/data jeniffer"
  },
  "engines": { "node": ">=22.0.0" },
  "devDependencies": {
    "@vercel/node": "^5.0.2"
  }
}
